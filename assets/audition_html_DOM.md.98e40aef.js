import{_ as a,c as t,o as e,U as o}from"./chunks/framework.b82ed5e4.js";const i="/ljtui2/dom树.webp",l="/ljtui2/含有js和css的页面渲染.webp",p=JSON.parse('{"title":"DOM","description":"","frontmatter":{},"headers":[],"relativePath":"audition/html/DOM.md","lastUpdated":null}'),r={name:"audition/html/DOM.md"},d=o('<h1 id="dom" tabindex="-1">DOM <a class="header-anchor" href="#dom" aria-label="Permalink to &quot;DOM&quot;">​</a></h1><h2 id="什么是dom-文档对象模型" tabindex="-1">什么是DOM，文档对象模型 <a class="header-anchor" href="#什么是dom-文档对象模型" aria-label="Permalink to &quot;什么是DOM，文档对象模型&quot;">​</a></h2><p>从网络传给渲染引擎的 HTML 文件字节流是无法直接被渲染引擎理解的，所以要将其转化 为渲染引擎能够理解的内部结构，这个结构就是 DOM。DOM 提供了对 HTML 文档结构 化的表述。 <img src="'+i+'" alt="dom树"> DOM 是表述 HTML 的内部数据结构，它会将Web页面和JavaScript脚本连接起来</p><h2 id="dom-树如何生成" tabindex="-1">DOM 树如何生成 <a class="header-anchor" href="#dom-树如何生成" aria-label="Permalink to &quot;DOM 树如何生成&quot;">​</a></h2><p>渲染引擎内部，有一个叫HTML 解析器（HTMLParser）的模块，它的职责就是负责将HTML 字节流转换为 DOM 结构。HTML 解析器并不是等整个文档加载完成之后再解析的，而是网络进程加载了多少数据，HTML 解析器便解析多少数据。</p><p>网络进程接收到响应头之后，会根据请求头中的 content-type字段来判断文件的类型，比如 content-type 的值是“text/html”，那么浏览器就会判断这是一个 HTML 类型的文件，然后为该请求选择或者创建一个渲染进程。渲染进程准备好之后，网络进程和渲染进程之间会建立一个共享数据的管道，网络进程接收到数据后就往这个管道里面放，而渲染进程则从管道的另外一端不断地读取数据，并同时将读取的数据“喂”给 HTML 解析器。你可以把这个管道想象成一个“水管”，网络进程接收到的字节流像水一样倒进这个“水管”，而“水管”的另外一端是渲染进程的 HTML 解析器，它会动态接收字节流，并将其解析为 DOM。</p><p>字节流转换为 DOM 需要三个阶段</p><ol><li><p>通过分词器将字节流转换为 Token</p><ul><li>V8 编译 JavaScript 过程中的第一步是做词法分析，将 JavaScript 先分解为一个个 Token。解析 HTML 也是一样的，需要通过分词器先将字节流转换为一个个 Token，分为 <strong>Tag Token 和文本 Token</strong>。上述 HTML 代码通过词法分析生成的 Token 如下所示：</li><li><strong>HTML 解析器(HTMLParser)维护了一个Token 栈结构</strong>，该 Token 栈主要用来计算节点之间的父子关系，在第一个阶段中生成的 Token 会被按照顺序压到这个栈中。</li><li>通过分词器产生的新 Token 就这样不停地压栈和出栈，整个解析过程就这样一直持续下去，直到分词器将所有字节流分词完成。</li></ul></li><li><p>Token 解析为 DOM 节点</p></li><li><p>并将DOM 节点添加到 DOM 树中。</p></li></ol><ul><li>HTML 解析器开始工作时，<strong>会默认创建了一个根为 document 的空 DOM 结构</strong>，同时会将一个 StartTag document 的 Token 压入栈底。然后经过分词器解析出来的第一个 StartTag html Token 会被压入到栈中，并创建一个 html 的 DOM 节点，添加到 document 上</li></ul><h2 id="javascript和css会不会影响dom树构建和渲染" tabindex="-1">JavaScript和css会不会影响DOM树构建和渲染 <a class="header-anchor" href="#javascript和css会不会影响dom树构建和渲染" aria-label="Permalink to &quot;JavaScript和css会不会影响DOM树构建和渲染&quot;">​</a></h2><ol><li>CSS不会阻塞DOM树的解析，但是会影响 JavaScript的运行</li><li>JavaScript 会阻止DOM树的解析</li><li>最终CSS（CSSOM）会影响DOM树的渲染，也可以说最终会影响渲染树的生成</li></ol><h2 id="javascript对dom树构建和渲染的影响" tabindex="-1">JavaScript对DOM树构建和渲染的影响 <a class="header-anchor" href="#javascript对dom树构建和渲染的影响" aria-label="Permalink to &quot;JavaScript对DOM树构建和渲染的影响&quot;">​</a></h2><ol><li>执行到 JavaScript 标签时，暂停整个 DOM 的解析，执行 JavaScript 代码，如果要下载JavaScript代码，下载过程会阻塞DOM 解析</li><li>Chrome 浏览器预解析操作，当渲染引擎收到字节流之后，会开启一个预解析线程，用来分析 HTML 文件中包含的 JavaScript、CSS 等相关文件，解析到相关文件之后，预解析线程会提前下载这些文件。</li></ol><h3 id="规避javascript线程阻塞dom现象" tabindex="-1">规避JavaScript线程阻塞DOM现象 <a class="header-anchor" href="#规避javascript线程阻塞dom现象" aria-label="Permalink to &quot;规避JavaScript线程阻塞DOM现象&quot;">​</a></h3><p>JavaScript 脚本设置为异步加载，通过 async 或 defer 来标记代码</p><h2 id="css对dom树构建和渲染的影响" tabindex="-1">CSS对DOM树构建和渲染的影响 <a class="header-anchor" href="#css对dom树构建和渲染的影响" aria-label="Permalink to &quot;CSS对DOM树构建和渲染的影响&quot;">​</a></h2><p>DOM 的过程中，如果遇到了 JavaScript 脚本，那么需要先暂停 DOM 解析去执行 JavaScript，因为 JavaScript 有可能会修改当前状态下的 DOM。不过在执行 JavaScript 脚本之前，如果页面中包含了外部 CSS 文件的引用，或者通过style 标签内置了 CSS 内容，那么渲染引擎还需要将这些内容转换为 CSSOM，因为JavaScript 有修改 CSSOM 的能力，所以在执行 JavaScript 之前，还需要依赖 CSSOM。 <strong>也就是说 CSS 在部分情况下也会阻塞 DOM 的生成</strong>。<strong>碰到js前先生成cssom在执行js然后继续构建DOM</strong><img src="'+l+'" alt="渲染"></p><p>当有外部css和js引入时，在接收到 HTML 数据之后的预解析过程中，HTML 预解析器识别出来了有 CSS 文件和 JavaScript 文件需要下载，然后就同时发起这两个文件的下载请求，需要注意的是，这两个文件的下载过程是重叠的，所以下载时间按照最久的那个文件来算。后面的流水线就和前面是一样的了，<strong>不管 CSS 文件和 JavaScript 文件谁先到达，都要先等到 CSS 文件下载完成并生成 CSSOM，然后再执行 JavaScript 脚本</strong>，最后再继续构建DOM，构建渲染树，绘制页面。<strong>CSS 文件下载完成之前有白屏。</strong></p><h2 id="domcontentloaded" tabindex="-1">DOMContentLoaded <a class="header-anchor" href="#domcontentloaded" aria-label="Permalink to &quot;DOMContentLoaded&quot;">​</a></h2><ol><li>文档解析完毕，当页面引用的所有 js 同步代码执行完毕，DOM树构建完成，触发 DOMContentLoaded 事件。</li><li>html 文档中的图片资源，js 代码中有异步加载的 css、js 、图片资源都加载完毕之后，load 事件触发。</li></ol><h2 id="dom文档加载的步骤为" tabindex="-1">DOM文档加载的步骤为： <a class="header-anchor" href="#dom文档加载的步骤为" aria-label="Permalink to &quot;DOM文档加载的步骤为：&quot;">​</a></h2><ol><li>解析HTML结构。</li><li>加载外部脚本和样式表文件。</li><li>解析并执行脚本代码。</li><li>DOM树构建完成。//DOMContentLoaded</li><li>加载图片等外部文件。</li><li>页面加载完毕。//load</li></ol><h2 id="网页的渲染" tabindex="-1">网页的渲染 <a class="header-anchor" href="#网页的渲染" aria-label="Permalink to &quot;网页的渲染&quot;">​</a></h2><ol><li>加载js然后html和css，设置defer和acync时js和html异步加载</li><li>html转换为dom，css转换为cssom</li><li>将DOM和CSSOM合并成一棵渲染render树</li><li>对渲染树进行reflow(回流，重排)，计算元素位置</li><li>绘制网页repaint(重绘)</li><li>根据浏览器提供各层的信息<a href="/ljtui2/audition/html/html.html#_12-合成层">合成图层</a>，显示到屏幕上</li></ol><h3 id="dom树的构建是文档加载完成开始的" tabindex="-1">DOM树的构建是文档加载完成开始的？ <a class="header-anchor" href="#dom树的构建是文档加载完成开始的" aria-label="Permalink to &quot;DOM树的构建是文档加载完成开始的？&quot;">​</a></h3><p>构建 DOM 树是一个渐进过程，为达到更好的用户体验，渲染引擎会尽快将内容显示在屏幕上，它不必等到整个 HTML 文档解析完成之后才开始构建 render 树和布局。</p><h3 id="render-树是-dom-树和-css-样式表构建完毕后才开始构建的" tabindex="-1">Render 树是 DOM 树和 CSS 样式表构建完毕后才开始构建的？ <a class="header-anchor" href="#render-树是-dom-树和-css-样式表构建完毕后才开始构建的" aria-label="Permalink to &quot;Render 树是 DOM 树和 CSS 样式表构建完毕后才开始构建的？&quot;">​</a></h3><p>这三个过程在实际进行的时候并不是完全独立的，而是会有交叉，会一边加载，一边解析，以及一边渲染。</p><h3 id="css-的解析注意点" tabindex="-1">CSS 的解析注意点？ <a class="header-anchor" href="#css-的解析注意点" aria-label="Permalink to &quot;CSS 的解析注意点？&quot;">​</a></h3><p>CSS 的解析是从右往左逆向解析的，嵌套标签越多，解析越慢。</p><h3 id="js-操作真实-dom-的代价" tabindex="-1">JS 操作真实 DOM 的代价？ <a class="header-anchor" href="#js-操作真实-dom-的代价" aria-label="Permalink to &quot;JS 操作真实 DOM 的代价？&quot;">​</a></h3><p>原生 JS 或 JQ 操作 DOM 时，浏览器会从构建 DOM 树开始从头到尾执行一遍流程，需要更新 10 个 DOM 节点就要从头到尾执行10遍，计算 DOM 节点坐标值等都是白白浪费的性能。即使计算机硬件一直在迭代更新，操作 DOM 的代价仍旧是昂贵的，频繁操作还是会出现页面卡顿，影响用户体验</p><h2 id="virtual-dom-就是一个js对象模拟的dom树" tabindex="-1">Virtual-DOM 就是一个JS对象模拟的DOM树 <a class="header-anchor" href="#virtual-dom-就是一个js对象模拟的dom树" aria-label="Permalink to &quot;Virtual-DOM 就是一个JS对象模拟的DOM树&quot;">​</a></h2><p><img src="https://juejin.cn/post/6844903895467032589" alt="参考这篇"> 虚拟 DOM 就是为了解决浏览器性能问题而被设计出来的。如前，若一次操作中有 10 次更新 DOM 的动作，虚拟 DOM 不会立即操作 DOM，而是将这 10 次更新的 diff 内容保存到本地一个 JS 对象中，最终将这个 JS 对象一次性 attch 到 DOM 树上，再进行后续操作，避免大量无谓的计算量。所以，用 JS 对象模拟 DOM 节点的好处是，页面的更新可以先全部反映在 JS 对象(虚拟 DOM )上，操作内存中的 JS 对象的速度显然要更快，等更新完成后，再将最终的 JS 对象映射成真实的 DOM，交由浏览器去绘制。</p><h3 id="virtual-dom的实现" tabindex="-1">Virtual-DOM的实现 <a class="header-anchor" href="#virtual-dom的实现" aria-label="Permalink to &quot;Virtual-DOM的实现&quot;">​</a></h3><ol><li>用 JS 对象模拟 DOM 树</li><li>diff算法比较两棵虚拟 DOM 树的差异</li><li>将两个虚拟 DOM 对象的差异应用到真正的 DOM 树</li></ol><h2 id="vue的virtual-dom" tabindex="-1">vue的Virtual-DOM <a class="header-anchor" href="#vue的virtual-dom" aria-label="Permalink to &quot;vue的Virtual-DOM&quot;">​</a></h2><h3 id="vnode-模拟-dom-树" tabindex="-1">VNode 模拟 DOM 树 <a class="header-anchor" href="#vnode-模拟-dom-树" aria-label="Permalink to &quot;VNode 模拟 DOM 树&quot;">​</a></h3><p>利用 _createElement 方法创建 VNode，它定义在 src/core/vdom/create-elemenet.js 中</p>',39),s=[d];function n(c,h,M,O,S,m){return e(),t("div",null,s)}const u=a(r,[["render",n]]);export{p as __pageData,u as default};
